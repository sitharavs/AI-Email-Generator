# AI-Email-Generator
E-mailed It! is a simple web app that generates clear, human-sounding emails from a prompt (ex: recruiter outreach, follow-ups, networking). Includes tone presets and optional “what to emphasize/avoid” instructions so you can ship emails fast without sounding templated.
